[A5] src/SeguridadApp/ProteccionRuta.tsx (dueño: módulo SeguridadApp, no A5): necesita `'use client';` en la 1ª línea y cambiar `import { Navigate, Outlet, useLocation, useNavigate } from 'react-router-dom'` -> `'@/compat/router'`. Hoy se reusa desde app/(protegido)/layout.tsx (Client Component) y react-router-dom aún resuelve (Fase 2 no lo ha quitado), pero al renderizarse FUERA del <BrowserRouter> del shell los hooks de react-router lanzarán en runtime; debe migrarse a la capa compat.
[A4] src/Paginas/Paquetes/Paquetes.tsx usa <SEO> de src/componentes/common/SEO (dueño: Agente 10). Ahora es redundante con el metadata server-side de app/paquetes/page.tsx; evaluar quitar el <SEO> client cuando el Agente 10 procese ese componente (no se tocó para mantener cambios quirúrgicos).
[A1] src/componentes/common/SEO.tsx (dueño: Agente 10): usa `useEffect` y NO tiene `'use client';`. Hoy lo consume src/Paginas/Inicio/Home.tsx (ya Client Component) y funciona por herencia, pero si otro agente lo importa desde un Server Component fallará. Recomendado: añadir `'use client';` en la 1ª línea.
[A1] src/componentes/Busqueda/ModalBusqueda.tsx (dueño: Agente 10): consumido por src/Paginas/404/Pagina404.tsx (ya Client Component, funciona por herencia). Si usa estado/efectos, añadir `'use client';` para que sea seguro importarlo también desde Server Components.
[A3] src/SeguridadApp/ProteccionRuta.tsx y ProteccionAdmin.tsx (dueño: módulo SeguridadApp, no A3): mismo blocker que reportó [A5]. A3 reusa ProteccionRuta vía src/Paginas/Tutoriales/GuardTutorialProtegido.tsx desde los layout.tsx de las rutas protegidas /tutoriales/[slug]/contenido y /tutoriales/[slug]/clase/[claseSlug]. Requiere que SeguridadApp cambie `react-router-dom` -> `@/compat/router` y añada `'use client';` (sin tocar el cuerpo). Hecho eso, las rutas protegidas de A3 funcionan sin cambios adicionales.
[A8] src/SeguridadApp/ProteccionAdmin.tsx (dueño: módulo SeguridadApp, no A8): mismo blocker [A5]/[A3]. A8 lo reusa TAL CUAL desde app/administrador/layout.tsx (Client Component) para proteger las 19 rutas /administrador/*. Importa `{ Outlet, useNavigate } from 'react-router-dom'`; al renderizarse fuera del <BrowserRouter> los hooks lanzarán en runtime. Requiere cambiar ese import -> `@/compat/router` y `'use client';` en la 1ª línea (sin tocar el cuerpo: ya soporta children).
[A8] src/componentes/paquetes/FormularioPaquete.tsx (dueño: Agente 10): consumido por src/Paginas/administrador/paquetes/crear/CrearPaquete.tsx y editar/EditarPaquete.tsx (ya Client Components, funciona por herencia). Si usa estado/efectos/eventos añadir `'use client';` para que sea seguro importarlo desde Server Components; revisar imports react-router-dom internos -> `@/compat/router`.
[A8] src/componentes/CrearContenido/WizardContenido.tsx (dueño: Agente 10): consumido por src/Paginas/administrador/crear-contenido/CrearContenido.tsx (ya Client Component, funciona por herencia). Si usa estado/efectos añadir `'use client';` y migrar cualquier import react-router-dom interno -> `@/compat/router`.
[A9] src/SeguridadApp/ProteccionAdmin.tsx (dueño: módulo SeguridadApp, no A9): mismo blocker ya reportado por [A8]/[A3]/[A5]. A9 tambien lo reusa TAL CUAL desde app/(protegido-admin)/layout.tsx (Client Component) para sus rutas /acordeon-pro-max*, /acordeon-3d-test, /acordeon-funcional-v1, /simulador-app. Con el fix de [A8] (import react-router-dom -> @/compat/router + 'use client'; sin tocar el cuerpo, ya soporta children) las rutas de A9 funcionan sin cambios adicionales.
