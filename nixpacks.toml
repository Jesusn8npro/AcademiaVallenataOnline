[variables]
    NODE_ENV = "production"
    NIXPACKS_METRI_MODE = "true"

[phases.setup]
    nixPkgs = ["nodejs-22_x", "python3", "pkg-config"]

[phases.install]
    cmds = ["npm install --include=dev --legacy-peer-deps"]

[phases.build]
    cmds = ["npm run build"]

[start]
    cmd = "npm run preview"
