
[phases.setup]
nixPkgs = ["nodejs_23"]

[phases.install]
cmds = ["npm ci --include=dev --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l tcp://0.0.0.0:${PORT:-3000}"
